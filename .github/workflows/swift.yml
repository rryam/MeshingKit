name: Build
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build:
    name: Build Package
    runs-on: macos-26  # Using macOS 26 beta for Xcode 26 testing
    steps:
      - uses: actions/checkout@v4
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26'  # Using Xcode 26 for latest features
      - name: Download Metal Toolchain
        run: xcodebuild -downloadComponent MetalToolchain
      - name: Build Swift Package
        run: swift build
      - name: Build Meshiin for iOS (Debug)
        run: xcodebuild -project "Sources/Meshin/Meshin.xcodeproj" -scheme "Meshin" -sdk iphonesimulator -configuration Debug -destination "generic/platform=iOS Simulator"
      - name: Build Meshiin for macOS (Debug)
        run: xcodebuild -project "Sources/Meshin/Meshin.xcodeproj" -scheme "Meshin" -sdk macosx -configuration Debug CODE_SIGNING_ALLOWED=NO CODE_SIGN_IDENTITY=""
